language: python
dist: trusty
notifications:
  email: false
python:
  - "2.7"
install: 
  - pip install pytest
  - pip install -e .

# command to run tests
script: 
  - py.test --pyargs robustsecretsharing